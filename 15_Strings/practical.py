text = 'London is the capital of Great Britain'
words = text.split()

# min_word = words[0]
# max_word = words[0]
# min_word_len = len(words[0])
# max_word_len = len(words[0])

# for word in words[1:]:
#     if len(word) > max_word_len:
#         max_word_len = len(word)
#         max_word = word
#     elif len(word) < min_word_len:
#         min_word_len = len(word)
#         min_word = word
        
# print(min_word, min_word_len)
# print(max_word, max_word_len)

# minimum = min([1, 32514, 135, 1, -131])
# maximum = max([1, 32514, 135, 1, -131])

# minimum = min(words, key=len)
# maximum = max(words, key=len)

# print(minimum)
# print(maximum)